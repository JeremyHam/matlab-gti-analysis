# matlab-gti-analysis
A MATLAB project that uses the GTI Vehicles dataset and extracts histogram of oriented gradients (HOG) and local binary patterns (LBP) for feature extraction and analysis using an SVM classifier.
